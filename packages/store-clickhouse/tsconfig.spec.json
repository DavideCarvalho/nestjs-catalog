{
  "//": "Typechecking for the specs, which the build config MUST exclude \u2014 they would otherwise be emitted into dist and published. Extends the build config so the two cannot drift on strictness, then inverts what it excludes.",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "declaration": false,
    "rootDir": "../.."
  },
  "include": [
    "src/**/*.spec.ts",
    "src/**/*.spec.tsx",
    "src/**/*.test.ts",
    "src/**/*.test.tsx",
    "test/**/*"
  ],
  "exclude": [
    "node_modules",
    "dist"
  ]
}
