---
"@dudousxd/nestjs-catalog": patch
---

Point `physicalColumn`'s note at `warehouse.store.ts`, the file that actually holds the copy it describes — `mysql-warehouse.store.ts` was renamed when the Postgres warehouse store landed.
