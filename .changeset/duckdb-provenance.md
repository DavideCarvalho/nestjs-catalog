---
'@dudousxd/nestjs-catalog-store-duckdb': patch
---

Republished so the package carries build provenance. 0.1.0 is identical in content
but was published without an attestation, so it cannot be verified against the
workflow that built it.
